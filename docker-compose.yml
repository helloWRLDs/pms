version: "3.9"

services:
  redis:
    image: redis:7.0.8
    container_name: "redis"
    restart: "always"
    ports:
      - 6379:6379
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf
    command:
      - "redis-server"